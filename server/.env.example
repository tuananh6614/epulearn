
# MySQL Database Configuration
# Make sure to create these credentials in your MySQL server
DB_HOST=localhost
DB_USER=root        # Change this to your MySQL username
DB_PASSWORD=        # Change this to your MySQL password (leave empty if no password)
DB_NAME=epu_learning

# Server Configuration
PORT=3000

# Instructions:
# 1. Rename this file to .env
# 2. Update the database credentials to match your MySQL setup
# 3. Make sure MySQL server is running
# 4. Create database by running: `mysql -u YOUR_USERNAME -p < database.sql`
