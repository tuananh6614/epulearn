
# MySQL Database Configuration
# Make sure to create these credentials in your MySQL server
DB_HOST=localhost     # MySQL server host, thường là localhost
DB_USER=root          # Tên người dùng MySQL của bạn (mặc định thường là root)
DB_PASSWORD=          # Mật khẩu MySQL của bạn (để trống nếu không có mật khẩu)
DB_NAME=epu_learning  # Tên database sẽ được tạo

# Server Configuration
PORT=3000             # Cổng mà server Express sẽ chạy, mặc định là 3000

# Note: No password encryption is used in this application
# Passwords are stored as plain text in the database

# Hướng dẫn cài đặt:
# 1. Đổi tên file này thành .env
# 2. Cập nhật thông tin đăng nhập MySQL theo thiết lập của máy bạn
# 3. Đảm bảo MySQL server đang chạy
# 4. Tạo database bằng lệnh: 
#    mysql -u YOUR_USERNAME -p < database.sql
#    (Thay YOUR_USERNAME bằng tên người dùng MySQL của bạn)
# 5. Chạy server bằng lệnh: npm run dev hoặc node server.js

# Lỗi thường gặp:
# - "Access denied for user 'root'@'localhost'": Kiểm tra lại tên người dùng và mật khẩu MySQL
# - "Connection refused": Kiểm tra MySQL server đã được khởi động chưa
# - "Unknown database": Chạy lệnh tạo database như hướng dẫn ở bước 4

